*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
# Local map authoring screenshots (source TSV/JSON are tracked under map-assets/)
app/map-assets/*.png
/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
# App signing secrets — NEVER commit these. Back them up separately (see keystore.properties.example).
keystore.properties
*.jks
*.keystore
# App passwords (developer key + default management password). NEVER commit.
# See secrets.properties.example.
secrets.properties
